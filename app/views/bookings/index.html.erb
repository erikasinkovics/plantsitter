<h1> My Plant-Sitting Requests:</h1>

<h2>Here are your booking requests:</h2>

<ul>
  <% @bookings.each do |booking| %>
    <li>  <%= link_to 'Booking', booking_path(booking) %> Start date: <%=booking.start_date%> End date: <%=booking.end_date%> Status: <%=booking.status%> </li>
      <%= render 'booking_card', {booking: booking} %>
  <% end %>
</ul>

