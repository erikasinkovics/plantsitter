<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <% if user.plant_sitter == true %>
      <li><%= link_to "#{user.first_name} #{user.last_name}", user_path(user) %></li>
    <% end %>
  <% end %>
</ul>
